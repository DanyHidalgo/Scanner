class Program {
    boolean entrada;
    int numeros;

    int suma(int valor1, int valor2){
        int res;  // Nombre de variable corregido
        res = valor1 + valor2;
        return res;
    }

    // @ Buenas

    /*
    Buenas tardes
    */
    
    void main(){
        int resultado1, resultado2;  // Nombre de variable corregido
        boolean resultado3;
        entrada = true;

        if (entrada == true){
            resultado1 = suma(3,2);
        }
        entrada = false;
        if (entrada == true){
            resultado1 = suma(3,2);  // Función "resta" no definida, la cambié a "suma"
        }
    }
}
